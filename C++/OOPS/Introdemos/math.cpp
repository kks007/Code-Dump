#include <iostream>
#include <math.h>
using namespace std;


int main()
{
    double a = 0;
    cin >> a ;

    // cout << sqrt(a) << endl;
    // cout << floor(a) << endl;
    // cout << ceil(a) << endl;
    // cout << abs(a);
    cout << sin(a);
    
}