#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, n, ans = 0, root;
    cin >> t;
    while(t--)
    {   ans = 0;
        cin >> n;
        cout << floor(sqrt(n)) << endl;
    }
}